# Production Environment Variables
# EC2 Backend URL (port 5000 as per backend-deploy.yml)
VITE_API_BASE_URL=https://d3pyfks9l1306x.cloudfront.net

# Firebase Configuration
# NOTE: These values should be set in GitHub Secrets for production builds
# The build process will inject them during deployment
# For local testing, copy these from your .env file

